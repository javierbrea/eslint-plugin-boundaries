{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "cspell-config",
  "projectType": "library",
  "tags": [
    "type:node:config"
  ],
  "implicitDependencies": [
    "eslint-config"
  ],
  "targets": {
    "cspell:config": {
      "cache": true,
      "inputs": ["default"],
      "outputs": [
        "{projectRoot}/**/*"
      ]
    }
  }
}
