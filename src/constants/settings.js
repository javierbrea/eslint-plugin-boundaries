const { PLUGIN_NAME } = require("./plugin");

const {
  NO_PRIVATE,
  ENTRY_POINT,
  ELEMENT_TYPES,
  EXTERNAL,
  PREFER_RECOGNIZED_TYPES,
  NO_IMPORT_NOT_RECOGNIZED_TYPES,
  NO_IMPORT_IGNORED,
} = require("./rules");

module.exports = {
  ELEMENTS: `${PLUGIN_NAME}/elements`,
  IGNORE: `${PLUGIN_NAME}/ignore`,
  RULE_ELEMENT_TYPES: `${PLUGIN_NAME}/${ELEMENT_TYPES}`,
  RULE_EXTERNAL: `${PLUGIN_NAME}/${EXTERNAL}`,

  // Deprecated
  TYPES: `${PLUGIN_NAME}/types`,
  ALIAS: `${PLUGIN_NAME}/alias`,

  // To be removed
  RULE_NO_PRIVATE: `${PLUGIN_NAME}/${NO_PRIVATE}`,
  RULE_ENTRY_POINT: `${PLUGIN_NAME}/${ENTRY_POINT}`,
  RULE_PREFER_RECOGNIZED_TYPES: `${PLUGIN_NAME}/${PREFER_RECOGNIZED_TYPES}`,
  RULE_NO_IMPORT_NOT_RECOGNIZED_TYPES: `${PLUGIN_NAME}/${NO_IMPORT_NOT_RECOGNIZED_TYPES}`,
  RULE_NO_IMPORT_IGNORED: `${PLUGIN_NAME}/${NO_IMPORT_IGNORED}`,
};
